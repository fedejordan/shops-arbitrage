/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['fravega.com', 'musimundo.com', 'oncity.com', 'localhost'],
    },
  }
  
  module.exports = nextConfig